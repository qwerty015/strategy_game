# Release notes

По одному файлу на билд, имя файла — это же значение `BuildVersion`
(`cmd/game/version.go`), например `ver_0.1_alpha_build_2026.31.08.md`.
Обновляйте `BuildVersion` перед каждой новой распространяемой сборкой и
добавляйте сюда соответствующий файл с описанием изменений.
