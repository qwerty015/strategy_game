# Дорожная карта

## Выполнено

Экономика и цепочки:
- базовая карта и размещение зданий, рецепты производства и буферы;
- Farm → Mill → Bakery → Tavern;
- Winery → Tavern (вино — обычная еда, 8 клеток виноградника, винодел);
- Свиноферма → мясной цех → харчевня (3 пшеницы → тушка → 2 колбасы);
- хижина лесоруба, лесоруб, добыча и доставка брёвен;
- Столярная мастерская → доски (1 бревно → 2 доски), профессия «столяр»;
- хижина рыбака, рыбак с лодкой, возобновляемая рыба в водоёмах;
- склад безлимитный и общий; можно ставить и удалять дополнительные склады;
- харчевня принимает любую еду (хлеб/рыба/вино/колбаса), юнит на месте
  случайно выбирает из реально доступного на этот тик;
- прямые перевозки производитель→потребитель в приоритете над складом;
- **реестр резервирования** (`internal/reservations`) — общий для всех
  контроллеров учёт "уже кем-то занято в этом тике", решает проблему
  "толпы слуг за одним заданием" без полноценной очереди;
- **приоритет снабжения по типу здания** — слайдер в инспекторе, ломает
  равенство между конкурирующими потребителями одного ресурса
  (например, мельница vs свиноферма за пшеницу);
- **Каменоломня и месторождения камня** — первый конечный (невозобновимый)
  ресурс. Карта при генерации получает 2-5 отдельных областей месторождений
  общей площадью 5-10% карты; клетка месторождения — 10000 камня. Каменотёс
  (по образцу лесоруба — не привязан к дорогам, сам ищет ближайшее
  месторождение с запасом) добывает по 1 единице камня за проход и сразу
  перерабатывает её в 2 каменных блока в буфере хижины — отдельного здания
  переработки не нужно. Несколько каменотёсов могут одновременно работать
  на одном месторождении (в отличие от дерева — очередь на него не нужна,
  запаса хватает на всех). Выработанная клетка убирается из мира и
  становится обычной свободной землёй. `save.GameState.StoneSeeded`
  отличает "старое сохранение без этой механики" (сгенерировать регион) от
  "современное сохранение, где весь камень уже выработан" (не подсовывать
  игроку камень обратно);
- **Стройка тратит ресурсы и требует строителя.** Постройка (кроме
  стартовых склада и дороги) — плоские 5 досок + 5 каменных блоков для
  любого здания, без исключений; дорога — 1 камень за клетку, без
  досок. Игрок ставит здание/дорогу → клетка резервируется как
  недостроенная (`building.ConstructionStage`) → строитель (новая
  профессия, до 3 штук, без своего здания — как слуга) приходит и роет
  фундамент бесплатно → как только фундамент готов, ждёт материалы, если
  их ещё нет → слуга привозит доски/камень → строитель достраивает →
  здание готово (склад дополнительно регистрируется как точка снабжения).
  Доставка стройматериалов — единственный вид рейса слуги, которому не
  нужна дорога (только по суше), иначе самую первую дорогу было бы
  невозможно построить. Фундамент, ожидание материалов и достройка имеют
  свои визуальные состояния; строитель — отдельный спрайт с молотком и
  чертежами. На старте у игрока 200 досок, 100 камня, 100 золота и по 100
  хлеба/рыбы/колбасы/вина;
- **Золото и наём отдельно от стройки.** Готовое здание больше не получает
  жителя автоматически — любой юнит (слуга, любая профессия, строитель)
  нанимается отдельно через вкладку "Найм" и
  стоит 1 золото. На старте у игрока 100 золота; карточка найма выглядит
  недоступной, если золота не хватает, даже когда здание для профессии
  свободно.
- **Уголь, золотая и железная руда + Плавильня.** Три новых конечных
  ресурса, по тому же принципу открытой добычи и `Reserve`, что и камень:
  своя хижина рудокопа (`MinerHut`), профессия «рудокоп» без привязки к
  дорогам. Угля заведомо больше, чем руды (доля карты при генерации:
  уголь 4-8%, железная руда 2-4%, золотая руда 1-2%, каждый — свои 2-5
  областей месторождений). Ни одно месторождение — камень, уголь, золотая
  или железная руда — не появляется ближе 20 клеток от изначального
  склада. Рудокоп не зацикливается на одном ресурсе:
  у каждого рудокопа своя циклическая очередь `miner.DefaultQuota`
  (1 золото → 1 железо → 3 угля → по кругу), с пропуском ресурса, если
  его месторождение сейчас недостижимо, без остановки всей очереди.
  Плавильня — одно здание с двумя рецептами (золотая руда + уголь →
  золото; железная руда + уголь → железо), сама выбирает, что плавить
  прямо сейчас, по тому, что реально есть на входе, и переключается
  по-честному (round-robin), не отдавая предпочтение одному рецепту
  постоянно (`Type.AltRecipes`, `Building.ActiveRecipe`). Первый
  прецедент многорецептурного здания в проекте.
- **Процедурная карта, генерируется заново при каждой новой игре.**
  Раньше карта была одной и той же зашитой раскладкой
  (`world.NewTestGrid`); теперь `NewGame` (и кнопка "Новая игра")
  генерируют карту 100×75 клеток (было 40×30) заново каждый раз, со
  свежим, никогда не сохраняемым зерном (`newMapSeed`, на основе текущего
  времени) — сама раскладка тайлов уже входит в сохранение как есть, так
  что зерну не нужно переживать загрузку, в отличие от зерна леса/рыбы.
  Одно море (8-14% площади карты) — единая сплошная полоса вдоль целиком
  одного случайно выбранного края карты, с неровной, но непрерывной
  береговой линией, а не блок, который лишь где-то касается края — чтобы
  к нему нужно было осознанно тянуть дорогу, а не наткнуться на пруд
  посреди города. Пахотные поля отдельно не генерируются (Farm может
  строиться на любой сухой клетке независимо от типа рельефа, отдельная
  "плодородная" текстура не нужна). Стартовый склад больше не на зашитых
  координатах и не всегда ближе к центру — `findWarehouseSpot` выбирает
  случайную клетку обычной травы со свободным местом под стартовую дорогу
  и отступом от края карты. Чащи — 3 зоны с плотностью деревьев ~45%
  поверх обычного равномерного разброса в 1% карты, а не вместо него.

Юниты и голод:
- дорожная логистика, слуги, точки доступа зданий, поиск пути по дорогам;
- фермеры, пекари, виноделы, свиноводы, мясники, плавильщики — рабочие
  юниты со своим зданием; лесоруб, рыбак, каменотёс и рудокоп — со своей
  механикой добычи; строитель — как слуга, без своего здания;
- **сытость как шкала 0–100%** вместо простого "голоден/нет": уходит по 1%
  за 10 тиков, поход в харчевню — при 20%, **смерть — при 0%**, для всех
  профессий без исключения;
- найм дополнительных слуг и профессий через левую панель (вкладка
  "найм"): профессия доступна, только если есть свободное здание нужного
  типа — это же ограничивает число NPC числом построек; строитель —
  исключение, фиксированный лимит в 3 штуки вместо привязки к зданию;
- увольнение выбранного слуги (завершает рейс, потом уходит);
- удаление выбранных зданий с безопасной отменой маршрутов, в т.ч. чужих
  (не только у "хозяина" здания);
- счётчики смертей и удалений в шапке;
- клик по зданию открывает здание (а не невидимого работника внутри);
  количество людей на здании — строкой в инспекторе, а не значком на карте;
  красная подсветка здания без работника.

Интерфейс и сохранение:
- сохранение и загрузка (включая роспись юнитов, сытость, приоритеты
  снабжения, отложенные восстановления деревьев/рыбы, зерно случайного
  выбора еды); миграция сохранений старого формата на новую шкалу сытости;
- визуальный каркас с боковыми панелями, кликабельное меню строительства,
  пауза и регулировка скорости, выбор зданий и юнитов;
- **вкладка "Опции" в левой панели** — живая смена языка (RU/EN, без
  перезапуска), своя копия регулировки скорости, кнопка **"Новая игра"**
  (полный сброс города с подтверждением) и **5 именованных слотов
  сохранения** (до 10 символов на имя), с подтверждением перед
  перезаписью уже занятого слота;
- **сохранение, загрузка, наём и выбор построек — только мышью**: клавиши S/L
  (быстрое сохранение/загрузка), 1-0/Q (выбор постройки по номеру), H (наём
  слуги) и Delete (удаление выбранного) убраны по прямому запросу пользователя;
  Esc отменяет открытый диалог;
- правая панель инспектора: состояние, маршрут, сытость, содержимое,
  людей внутри, приоритет снабжения;
- медленный случайный рост деревьев с лимитом 1% карты и сохранением
  очереди восстановления;
- базовый единый набор оригинальных 64×64 текстур рельефа, зданий и юнитов;
- интеграционный тест полной производственной цепочки.

## В работе

- автопереходы текстур рельефа и дорожных соединений;
- визуальные состояния здания и декоративные объекты.

## Следующий этап

- визуальная подсветка маршрута выбранного юнита (сам выбранный объект уже
  подсвечивается — `ui.DrawSelectionMarker` — но не рисует линию маршрута;
  заодно у Каменотёса/Строителя/Рудокопа нет подсветки вообще, только у
  здания/слуги/villager/лесоруба/рыбака — это тоже нужно закрыть заодно).

## После интерфейсного этапа

- декоративные слои карты;
- автосоединение графики дорог;
- тени и эффекты зданий;
- улучшение спрайтов юнитов;
- отдельная настройка масштаба камеры;
- полировка звука и обратной связи.

## Будущие цепочки и карта (пока идея, не спланировано детально)

Деревообработка:
- ~~столярка (Carpentry)~~ — сделано: Столярная мастерская + профессия
  «столяр», рецепт 1 бревно → 2 доски (`resource.Plank`). У здания и
  работника есть собственные спрайты;
- ~~стройка тратит доски и камень~~ — сделано, см. «Выполнено»:
  строитель (до 3 штук), два этапа (бесплатный фундамент → достройка
  после доставки материалов), стройматериалы — единственная доставка
  слугой не по дороге.

Добыча и конечные ресурсы:
- ~~камень (Каменоломня)~~ — сделано, см. «Выполнено»: конечный ресурс без
  восстановления, 2-5 областей месторождений при генерации карты, каменотёс
  добывает и сразу перерабатывает в каменные блоки;
- ~~уголь, железная руда, золотая руда + Плавильня~~ — сделано, см.
  «Выполнено»: открытая добыча по тому же принципу, что и камень,
  циклическая очередь рудокопа и многорецептурная Плавильня.

Живность:
- дикие звери — мировые объекты/точки спавна, вероятно в лесу;
- охотники — новая профессия, добывает животных (мясо и/или шкуры?) —
  по аналогии с рыбаком.

Соперник-строитель (требует решения, конфликтует с текущим дизайном):
- идея — не боевой ИИ-противник, а "компьютер", который просто строит
  свой посёлок по тем же правилам игры (вторая экономика на той же или
  соседней карте, без атак и юнитов-солдат);
- **это прямо противоречит текущему заявленному дизайну** в `AGENTS.md`
  ("без ИИ-противников, без боёв") — при реализации нужно либо явно
  пересмотреть это решение и обновить `AGENTS.md`, либо переформулировать
  идею так, чтобы она не была "противником" в смысле, от которого
  осознанно отказались в начале проекта (например, просто фоновый
  посёлок-соперник по показателям, не влияющий на игрока напрямую).

Карта и генерация:
- ~~карта большего размера~~ — сделано, см. «Выполнено»: 100×75 (было
  40×30), сама карта теперь ещё и **генерируется заново при каждой новой
  игре**, а не одна и та же зашитая раскладка. Позже уменьшена обратно
  вдвое до 50×38 по прямой просьбе пользователя — см. текущий размер в
  `AGENTS.md`/README.md, не в этой исторической записи;
- ~~водоёмы генерируются у края карты~~ — сделано, см. «Выполнено»: одно
  море 8-14% площади карты, растёт внутрь от случайно выбранного края;
- ~~чащи~~ — сделано, см. «Выполнено»: 3 зоны повышенной плотности
  деревьев поверх обычного равномерного разброса.
